# Python_Guess_the_number
A Python project that takes a number from the user and generates a random number between 0 and the number entered by the user. The user then has to guess the generated number
